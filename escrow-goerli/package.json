{
  "name": "escrow-goerli",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ canp0y/escrow-goerli",
    "create-local": "graph create --node http://localhost:8020/ canp0y/escrow-goerli",
    "remove-local": "graph remove --node http://localhost:8020/ canp0y/escrow-goerli",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 canp0y/escrow-goerli",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.48.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
